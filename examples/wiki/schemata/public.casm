module public in casm;

type NewWiki = {
    "filename": string(pattern="[a-zA-Z_][a-zA-Z0-9_-]*"),
};

type WikiSkele = {
    "title": string,
    "body": string,
};

type WikiFile = {
    "body": string,
};

