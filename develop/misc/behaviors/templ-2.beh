= foreach

== 説明

なんでこれが例外投げるかわからん。

== !exec

|= command       |= params           |= outputCond |= exception |
| request        | /foreach.html| true        |            |

注: requestの引数にはアプリケーション名を入れない。以下は404エラー

|= command       |= params           |= outputCond |= exception |
| request        | /misc/foreach.html|  true     |          |
